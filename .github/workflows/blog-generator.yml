name: 🚀 Auto Blog Generator

on:
  schedule:
    - cron: '0 9 * * *' # Roda todos os dias às 09:00 UTC
  workflow_dispatch: # Permite execução manual

jobs:
  generate-article:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      # PASSO 1: Instala todas as dependências necessárias antes de rodar o Python
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install google-genai requests pillow youtube-transcript-api
      
      # PASSO 2: Roda o gerador com TODAS as suas variáveis originais mapeadas
      - name: Generate article
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          COMPANY_NAME: "Masters SEO"
          COMPANY_WEBSITE: "https://masters-seo.github.io/"
          COMPANY_LOCATION: "Brasil"
          COMPANY_DESC: "Hub de análises independentes sobre os Experts do mercado de SEO"
        run: |
          python .github/scripts/generate_article.py
      
      # PASSO 3: Salva os posts criados e as imagens geradas no repositório
      - name: Commit changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add _posts/*.md || true
          git add assets/img/posts/*.jpg || true
          git diff --quiet && git diff --staged --quiet || (git commit -m "Add auto-generated article and assets $(date +'%Y-%m-%d')" && git push)
